# Design Spec Pointer

The canonical design-system spec for this project lives in `design_inspo/DESIGN.md`.

This root file exists as a stable entry point for contributors and agents. Update the canonical file first, then only expand this pointer if the repo later needs a distinct root-level summary.
