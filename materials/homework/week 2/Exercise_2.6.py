"""
Assume that you already have a variable called 'x', which is a dictionary.

Print a list of the values of the dictionary, sorted in descending order
of the corresponding dictionary keys.

For example:
If x = {1: 3, 3: 4, 2: 5} your program should print:
[4, 5, 3]
"""

# Hint 1: It never hurts to start with an empty list. Pick a name for it that
# you like. But not 'list'! That would have unpredictable consequences for the
# universe...

# Hint 2: Check out the 'sorted' function. It takes a collection of values and
# returns them as elements in a sorted list.

# Hint 3: Okay, now how do we build a list of dictionary values in the order 
# of the sorted keys? One-by-one (for now). Think about looping!

# Hint 4: And don't forget to print out the result, of course.
