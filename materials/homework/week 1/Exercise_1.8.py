"""
Assume that you already have a variable called 'x', which contains
a list with two elements. Each element is a further list with two elements: 
either 0 or 1.

Print a list that has the same structure as x, but all the 0's should be changed into 1's and all 1's into 0's.

For example: 
If x = [[1, 1], [1, 0]], then your program should print:
[[0, 0], [0, 1]]
"""

# Hint 1: did you know that 1 - 0 = 1 and 1 - 1 = 0?
