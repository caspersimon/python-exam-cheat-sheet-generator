"""
Assume that you already have a variable called 'x' that contains a string 
value.

Print a string that equals 3 times the value in 'x', where every
occurence of the value of 'x' is separated by the string '-3-'.  

For example:
If x = 'abc', your program should print:
abc-3-abc-3-abc
"""

# Hint 1: how do you concatenate (= join together) two strings?

# Hint 2: what happens when you use the multiplication operator between an 
# integer and a string?

# Hint 3: so then what combination of the two things above gives you what you
# need?
